# happygreenn Novel Web App

## Setup
```bash
python -m venv venv
venv\Scripts\activate   # Windows
pip install -r requirements.txt
copy .env.example .env
python run.py
```

Default admin:
- Email: author@happygreenn.com
- Password: admin123

Change these in `.env` before deployment.
# happygreenn
